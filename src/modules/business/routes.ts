const routePath = "api/v1/business";
const routePathAuth = "api/v1/auth/business";

export const routes = {
  business: {
    root: `${routePath}`,
  },
  business_auth: {
    root: `${routePathAuth}`,
  },
};
